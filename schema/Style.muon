# Style.muon schema
:::
# Style a signature
sig: [table]
   # Whether or not the time signature should use a special symbol (C for 4/4).
   # Default=false
   time_symbol: bool?
   # Text that should show up rather than "beat = BPM" marking.
   tempo: text?
   # Text that should show up rather than default.
   # "eighth eighth = triplet quarter, triple eighth"
   swing_text: text?
:::
